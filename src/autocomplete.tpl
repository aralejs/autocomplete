<div class="{{prefix}}">
    <ul class="{{prefix}}-ctn" data-role="list">
        {{#each list}}
            <li class="{{../prefix}}-item">{{highlightItem ../prefix}}</li>
        {{/each}}
    </ul>
</div>
